{
  "mcpServers": {
    "power-platform": {
      "command": "npx",
      "args": ["-y", "@emin-bit/power-platform-mcp"],
      "env": {
        "PAC_MCP_SAFE_MODE": "on",
        "PAC_MCP_VERBOSE": "0",
        "MCP_TIMEOUT": "600000"
      }
    }
  }
}
